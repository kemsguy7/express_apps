This is a bare ecommerce project built with expressJs and sqlite database

To run the project, on your terminal, run `npm install` then `npm start` 

Point your browser to port 3000 to view on the browser